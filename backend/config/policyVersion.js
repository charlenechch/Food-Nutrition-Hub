// backend/config/policyVersion.js

// Update this number (e.g. 1 → 2) when Privacy Policy or T&C are significantly changed — this will prompt ALL users to re-agree on their next login. Do NOT change for minor edits like typos or grammar. Remember to update the frontend src/config/policyVersion.js to the same number as well.
const CURRENT_POLICY_VERSION = 2;

module.exports = { CURRENT_POLICY_VERSION };